# @Author: shifath.nafis
# @Date:   2023-03-31 07:02:00
# @Last Modified by:   shifath.nafis
# @Last Modified time: 2023-05-18 12:16:45
#
# This is the virtual environment specification file
# It can be generated by running the following conda command: 
# conda env export --no-builds --from-history | grep -v "name\|prefix" > environment.yml
# --------------------------------------------------------------------
name: faisal-env
channels:
  - defaults
dependencies:
  - python=3.8
  - seaborn
  - scikit-learn=0.24
  - jupyter
  - numpy
  - matplotlib
  - jupyterlab
  - pandas
  - plotly=4.12.0
  - openpyxl
  - imbalanced-learn
  - pip:
      wordcloud==1.9.2